# CI Failure Inventory: run 28685258279

Status: evidence
Current tracker: ../../tracker.md

Run: <https://github.com/snazy/quarkus/actions/runs/28685258279?pr=184>  
Branch/SHA: `gradle-app-cc` / `366e0373ec8b73b7ab254f4e89a35369a8e48e32`  
Local evidence directory: `.agents/gracle-cc-work/ci-logs/run-28685258279/`

Scope requested:

- `Gradle Tests - JDK 25`
- `JVM Integration Tests - JDK 17`
- `JVM Tests - JDK 21`

## Job: Gradle Tests - JDK 25

Status: failed in the Maven `Build` step.

Build report:

- Project: `Quarkus - Integration Tests - Gradle tooling`
- Failure: `maven-surefire-plugin:test` on
  `quarkus-integration-test-gradle-plugin`
- Evidence: `extracted/gradle-jdk25/target/build-report.json`

Failed report groups:

- `io.quarkus.gradle.builder.QuarkusModelBuilderTest`: all five model-loading
  tests error while serializing the tooling model result. Root exception in XML:
  `java.io.NotSerializableException:
  io.quarkus.gradle.dependency.PlatformImportsBuildService$SynchronizedPlatformImports`.
  This is almost certainly branch-caused by the recent build-service wrapper:
  tooling API build actions must return a serializable model.
- `io.quarkus.gradle.DeclaredDependenciesMinimalTest.generateAppModel`:
  assertion failure, generated model is `null`.
- `io.quarkus.gradle.ConditionalDependenciesTest`: seven failures/errors. The
  prod-mode model path fails resolving
  `:quarkusProdBaseRuntimeClasspathConfigurationCopy` through
  `QuarkusComponentVariants.processConfiguration(...)`. Other methods fail
  Gradle wrapper builds.
- Composite-build extension tests:
  `BasicCompositeBuildExtensionQuarkusBuildTest`,
  `MultiCompositeBuildExtensionsQuarkusBuildTest`, and
  `TestCompositeBuildWithExtensionsTest` fail Gradle builds.
- Gradle extension unit fixture:
  `extensions/simple-extension/deployment` fails its generated Gradle test with
  `Failed to create the application model for null`, caused by
  `Failed to determine the Maven artifact associated with the application
  .../extensions/simple-extension/deployment`.
- Many dev-mode tests report `BROKEN: quarkusDev mode has terminated`,
  including basic application/library, composite build, Jandex, included build,
  KMP, and extension dependency cases. These need grouping by root cause after
  the model/extension failures above are fixed, because a bad generated app
  model can cascade into dev-mode startup failures.

Primary branch-caused buckets:

1. `CI-GRADLE-01` (fixed by `238454c99ef Fix Gradle tooling model platform imports serialization`):
   `PlatformImportsBuildService.SynchronizedPlatformImports` was not
   serializable and leaked into the tooling model result.
2. `CI-GRADLE-02` (fixed by `50fbc5e0be0 Fix declared dependency POM resolution in Gradle tasks`):
   `QuarkusDeclaredDependenciesTask` could produce unresolved declared
   dependency data in clean Gradle integration-test runs. The injected
   `DependencyHandler` was not tied to the consuming project's repositories
   once the task was isolated for configuration cache, and Gradle 8.14 also
   failed to reload the provider-backed POM file collection when it contained a
   JDK immutable collection value.
3. `CI-GRADLE-03` (partially fixed):
   new extension deployment marker/plugin shape breaks clean Gradle fixtures
   that do not apply the deployment plugin, including generated extension tests
   and composite builds.
   - `135e927eb5f Avoid eager extension dependency jar inspection` fixes clean
     publication/configuration-cache failures where `ExtensionDescriptorTask`
     inspected runtime dependency jars during input snapshotting before the
     producer jar task had run.
   - `03fab03fe84 Avoid immutable collections in extension validation providers`
     fixes Gradle 9.6 configuration-cache reload failures in
     `ValidateExtensionTask` provider-backed file collections.
   - `173f82c72f0 Wire extension deployment tests to generated app models`
     fixes the generated extension unit fixture by passing the serialized test
     application model through a task-input-aware JVM argument provider and by
     applying the deployment plugin in the clean fixture.
4. `CI-GRADLE-04` (partially fixed): dev-mode failures
   split into several root causes after the model/extension buckets were fixed.
   - `CI-GRADLE-04A` (fixed by `8052b3205dc Fix Gradle dev mode project dependency models`):
     project dependencies in the
     serialized dev application model were marked as reloadable workspace
     modules without an actual `WorkspaceModule`. This crashes
     `DependenciesFilter.getReloadableModules()` during dev-mode startup. The
     same bucket also exposed Gradle configuration-cache task-graph calculation
     failures where pending configuration providers were realized while Gradle
     was iterating consumable variants for project dependencies.
   - `CI-GRADLE-04B` (fixed by `d6f0d0cd386 Restore quarkusDev dependency declarations`):
     `QuarkusDevDependencyDevModeTest` fails on Gradle 9 because the public
     `quarkusDev(...)` configuration is registered as a resolvable,
     non-declarable configuration.
   - `CI-GRADLE-04C` (fixed by `cb4ed704ce7 Include generated source paths in Gradle project descriptors`):
     `CompileOnlyDependencyDevModeTest` starts after the project-model fix, but
     live reload does not regenerate the annotation processor output after
     changing the compile-only entity source.

Proof for the `CI-GRADLE-04A` fix:

- `./mvnw -f devtools/gradle/pom.xml install -DskipTests`
- `./mvnw -f integration-tests/gradle/pom.xml -Dtest=io.quarkus.gradle.devmode.BasicMultiModuleProjectDevModeTest test`
- Broader dev-mode subset:
  `BasicMultiModuleProjectDevModeTest`,
  `BasicJavaLibraryModuleDevModeTest`,
  `MultiModuleNamedInjectionDevModeTest`,
  `CompileOnlyDependencyDevModeTest`,
  `QuarkusDevDependencyDevModeTest`, and
  `MultiModuleWithNoTestModuleDevModeTest`. The first, second, third, and
  sixth tests pass; the remaining two failures are tracked as
  `CI-GRADLE-04B` and `CI-GRADLE-04C`.

Proof for the `CI-GRADLE-04B` fix:

- `./mvnw -f devtools/gradle/pom.xml install -DskipTests`
- `./mvnw -f integration-tests/gradle/pom.xml -Dtest=io.quarkus.gradle.devmode.QuarkusDevDependencyDevModeTest test`
- `./mvnw -f integration-tests/gradle/pom.xml -Dtest=io.quarkus.gradle.devmode.QuarkusDevDependencyDevModeAnalyticsTest test`

Proof for the `CI-GRADLE-04C` fix:

- `./mvnw -f devtools/gradle/pom.xml install -DskipTests`
- `./mvnw -f integration-tests/gradle/pom.xml -Dtest=io.quarkus.gradle.devmode.CompileOnlyDependencyDevModeTest test`

Broad dev-mode proof after all `CI-GRADLE-04` fixes:

- `./mvnw -f integration-tests/gradle/pom.xml -Dtest=io.quarkus.gradle.devmode.BasicMultiModuleProjectDevModeTest,io.quarkus.gradle.devmode.BasicJavaLibraryModuleDevModeTest,io.quarkus.gradle.devmode.MultiModuleNamedInjectionDevModeTest,io.quarkus.gradle.devmode.CompileOnlyDependencyDevModeTest,io.quarkus.gradle.devmode.QuarkusDevDependencyDevModeTest,io.quarkus.gradle.devmode.MultiModuleWithNoTestModuleDevModeTest test`
- Result: default Gradle profile passed 6 tests, Gradle 8.14 wrapper profile
  passed 6 tests, no failures or errors.

2. `CI-GRADLE-05A`: composite extension fixtures did not publish the deployment
   marker variant from their deployment modules.

   Clean included-build fixtures under `basic-composite-build-extension-project`,
   `multi-composite-build-extensions-project`, and
   `system-props-as-build-time-config-source` only applied the runtime extension
   plugin. After the runtime extension plugin started resolving the dedicated
   deployment marker variant, those deployment projects still exposed only
   regular Java variants, so Gradle reported `No matching variant` for
   `quarkusDeploymentMarker`.

   The fix is to apply `io.quarkus.extension.deployment` in the fixture
   deployment modules and declare that plugin ID in each included extension
   build's `pluginManagement` block.

   Proof before committing the fixture fix:

   - `./mvnw -f integration-tests/gradle/pom.xml -Dtest=io.quarkus.gradle.BasicCompositeBuildExtensionQuarkusBuildTest,io.quarkus.gradle.MultiCompositeBuildExtensionsQuarkusBuildTest,io.quarkus.gradle.TestCompositeBuildWithExtensionsTest,io.quarkus.gradle.SystemPropsAsBuildTimeConfigSourceTest test`
   - Result: `BasicCompositeBuildExtensionQuarkusBuildTest`,
     `MultiCompositeBuildExtensionsQuarkusBuildTest`, and
     `TestCompositeBuildWithExtensionsTest` passed. The remaining
     `SystemPropsAsBuildTimeConfigSourceTest` failure changed to the separate
     configuration-cache serialization failure tracked as `CI-GRADLE-05B`.

3. `CI-GRADLE-05B`: `SystemPropsAsBuildTimeConfigSourceTest` failed while
   loading the configuration cache for `quarkusGenerateTestAppModel`.

   Evidence after the `CI-GRADLE-05A` fixture fix:

   - `Could not load the value of field resources of
     io.quarkus.bootstrap.workspace.DefaultArtifactSources ...`
   - Root cause in the Gradle report: `java.io.InvalidObjectException: null
     array`

   Cause: the serialized Gradle project descriptor passed JDK immutable list
   implementations into `DefaultArtifactSources`. Gradle's configuration-cache
   Java object serialization did not round-trip those lists safely in this
   nested model.

   Fix: pass regular `ArrayList` instances from `ProjectDescriptorBuilder` for
   conventional source/resource descriptors and test source/resource
   descriptors.

   Proof:

   - `./mvnw -f devtools/gradle/pom.xml install -DskipTests`
   - `./mvnw -f integration-tests/gradle/pom.xml -Dtest=io.quarkus.gradle.SystemPropsAsBuildTimeConfigSourceTest test`
   - Result before the next fix: the configuration-cache serialization failure
     disappeared and Gradle reported `0 problems were found storing the
     configuration cache`; the build then failed later on the separate implicit
     dependency tracked as `CI-GRADLE-05C`.

4. `CI-GRADLE-05C`: `SystemPropsAsBuildTimeConfigSourceTest` then failed
   because `ValidateExtensionTask` read the deployment marker output without
   preserving the producing task dependency.

   Evidence:

   - `Task ':example-extension:example-extension:validateExtension' uses this
     output of task
     ':example-extension:example-extension-deployment:quarkusExtensionDeploymentMarker'
     without declaring an explicit or implicit dependency.`

   Cause: `ValidateExtensionTask.setDeploymentMarker()` converted the marker
   configuration to raw files inside a provider by calling
   `deploymentMarker.getFiles()`. That stripped Gradle's producer metadata from
   the task input.

   Fix: keep the conditional input behavior but return the deployment marker
   configuration itself when local deployment validation is enabled, and return
   an empty file collection when it is disabled.

   Proof:

   - `./mvnw -f devtools/gradle/pom.xml install -DskipTests`
   - `./mvnw -f integration-tests/gradle/pom.xml -Dtest=io.quarkus.gradle.SystemPropsAsBuildTimeConfigSourceTest test`
   - Result: passed, with Gradle reporting `0 problems were found storing the
     configuration cache`.

Combined proof after `CI-GRADLE-05A`, `CI-GRADLE-05B`, and `CI-GRADLE-05C`
fixes:

- `./mvnw -f integration-tests/gradle/pom.xml -Dtest=io.quarkus.gradle.BasicCompositeBuildExtensionQuarkusBuildTest,io.quarkus.gradle.MultiCompositeBuildExtensionsQuarkusBuildTest,io.quarkus.gradle.TestCompositeBuildWithExtensionsTest,io.quarkus.gradle.SystemPropsAsBuildTimeConfigSourceTest test`
- Result: 4 tests, 0 failures, 0 errors, 0 skipped. The Gradle builds in these
  tests stored configuration-cache entries successfully.

## Job: JVM Tests - JDK 21

Status: failed in the Maven `Build` step.

Build report:

- Project: `Quarkus - Gradle Plugin`
- Failure: `exec-maven-plugin:exec (gradle)` on
  `io.quarkus.gradle.plugin`
- Evidence: `extracted/jvm-jdk21/target/build-report.json`

Available artifacts:

- No `TEST-*.xml` failures were present under
  `extracted/jvm-jdk21/devtools/gradle`.
- The useful evidence is in TestKit daemon logs under
  `extracted/jvm-jdk21/devtools/gradle/*/build/tmp/test/work/.gradle-test-kit/`.

Observed failure signatures:

- `gradle-extension-deployment-plugin` daemon log shows a failed marker
  resolution:
  `Could not determine the dependencies of task ':runtime:resolveDeploymentMarker'`
  because `:runtime:deploymentMarker` could not resolve project `:deployment`.
  Gradle reports `No matching variant of project ':deployment' was found`; the
  consumer requested attribute `io.quarkus.extension.deployment=true` and
  category `quarkus-extension-deployment-marker`, but the producer only exposed
  normal Java/verification variants.
- The same daemon log later records `AmbiguousVariantsFailure` entries, likely
  from negative/diagnostic variant tests. These need local test reproduction
  before changing behavior.
- `gradle-application-plugin` daemon log contains successful
  `quarkusGoOffline`/declared-dependency task execution but repeatedly logs
  `Unable to resolve effective model for jakarta.inject:jakarta.inject-api:2.0.1:
  Could not resolve POM ...`. The visible excerpt ends with a successful build,
  so this is evidence to inspect, not yet an identified job-failure root cause.

Primary branch-caused bucket:

1. `CI-JVM21-01` (fixed by `4462c19939b Declare dev mode annotation processor path as classpath`):
   local reproduction showed the actual job failure was
   `:gradle-application-plugin:validatePlugins`, not the negative marker test
   noise in daemon logs. `QuarkusDev.mainSourceSetAnnotationProcessorPath`
   inherited into `QuarkusTest` and `QuarkusRemoteDev` was an `@InputFiles`
   property without a normalization annotation.

Ignored noise:

- `independent-projects/enforcer-rules/target/it/*/build.log` contains
  intentional failing invoker builds for enforcer-rule tests. The build report
  marks `Quarkus - Enforcer Rules` successful, so these are not the job failure.

## Job: JVM Integration Tests - JDK 17

Status: failed in the Maven `Build` step.

Build report:

- `Quarkus - Integration Tests - Hibernate Reactive - MySQL - With Agroal and
  Flyway`: surefire failure.
- `Quarkus - Integration Tests - Hibernate - Tenancy - Schema`: surefire
  failure.
- Evidence: `extracted/jvm-it-jdk17/target/build-report.json`

Failed tests:

- `io.quarkus.it.hibernate.reactive.mysql.HibernateReactiveMySQLTest.reactivePersist`
  fails during Quarkus startup. Root exception:
  `org.flywaydb.core.api.FlywayException: Found non-empty schema(s) quarkus but
  no schema history table. Use baseline() or set baselineOnMigrate to true...`
- `io.quarkus.it.hibernate.multitenancy.fruit.HibernateTenancyFunctionalityTest.testGetFruitsDefaultTenant`
  fails during Quarkus startup. Root exception:
  `ERROR: schema "base" already exists` while executing
  `db/migration/V1.0.0__init.sql`.

Current assessment:

- These look unrelated to the Gradle plugin changes. They are database/Flyway
  startup failures in non-Gradle integration-test modules.
- Do not fix in the Gradle workstream without additional evidence tying them to
  the branch. Recommended handling is to compare against adjacent CI runs or
  rerun only these modules before treating them as branch regressions.

## Next Proof Steps

1. Re-run a targeted Gradle integration test set covering the fixed buckets:
   `QuarkusModelBuilderTest`, `DeclaredDependenciesMinimalTest`,
   `ConditionalDependenciesTest`, and `ExtensionUnitTestTest`.
2. Run or inspect the remaining Gradle JDK25 failed classes not covered by the
   fixed buckets, especially composite-build extension dev-mode/build tests and
   `SystemPropsAsBuildTimeConfigSourceTest`.
3. Treat the two JVM integration database/Flyway failures as probable unrelated
   CI/environment flakiness until a branch-specific reproducer exists.
